//😺
//😸
//😹


/*
let gatos = ["😺", "😸", "😹"]
let cantidadDeGatos = 50           // ingresar cantidad de gatos
let contador = 0

for (let i = 0; i < cantidadDeGatos; i++) {
    if (contador == 0) {
        let respuesta = "Gatos # " + (i+1) + ":" + gatos[contador]
        console.log(respuesta);
        contador++

    } else if (contador == 1) {
        let respuestaDos = "Gatos # " + (i+1) + ":" + gatos[contador]
        console.log(respuestaDos);
        contador++

    } else {
        let respuestaTres = "Gatos # " + (i+1) + ":" + gatos[contador]
        console.log(respuestaTres);
        contador = 0

    }
    
}  

*/

// =========================================================================
// =========================================================================


/*
   let cantidadDePasos = 10           // ingresar pasos  
   let cantidadDeGatosDos = 6       // ingresar gatos 

for (let index = 0; index < cantidadDeGatosDos; index++) { 
    let respuesta = "Gatos # " + (index+1) + ": 🐈" 
    for (let indexDos = 0; indexDos < cantidadDePasos; indexDos++){
        respuesta += "🐾"
    }
    console.log(respuesta);
} 

*/

// =========================================================================
// =========================================================================

//🐈‍⬛

/*


let emojis = ["🐈", "🐈‍⬛" ]

let cantidadDePasos = 30     // ingresar pasos  
let cantidadDeGatosDos = 30      // ingresar gatos 
let contador = 0




for (let index = 0; index < cantidadDeGatosDos; index++)
if (contador == 0) { 
    let respuesta = "Gatos # " + (index+1) + emojis[contador] 
    for (let indexDos = 0; indexDos < cantidadDePasos; indexDos++){
        respuesta += "🐾"
    }
    console.log(respuesta);
    contador++
}   else if (contador == 1) {
    let respuesta = "Gatos # " + (index+1) + emojis[contador]
    for (let indexDos = 0; indexDos < cantidadDePasos; indexDos++){
        respuesta += "🐾"
    }
    console.log(respuesta);
    contador = 0
    
}

*/

// =========================================================================
// =========================================================================